/// Display mode used by [ListTableScaffold] and [ViewModeSwitcher].
enum DataViewMode {
  /// Card-style vertical list (existing dashboard behavior).
  list,

  /// Compact tabular layout with sortable headers (tablet/desktop only).
  table,
}
